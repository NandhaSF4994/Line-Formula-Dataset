train: ../images/train
val: ../images/val

# whether it is coco dataset, only coco dataset should be set to True.
is_coco: False

nc: 2   # number of classes

names: ['Underline','Strikeout','Formula']# class names 
